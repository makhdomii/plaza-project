{
  "name": "back",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/back",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/back"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/back"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx-extend/strapi:serve",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "@nx-extend/strapi:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "back:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "back:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "websocket:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/websocket/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/back/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
